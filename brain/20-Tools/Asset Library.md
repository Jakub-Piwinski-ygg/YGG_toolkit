---
type: tool
tool: Asset Library
category: 🏗️ Asset Pipeline
status: shipped
updated: 2026-06-14
tags: [asset-pipeline, library]
---

# Asset Library

Curated 2D asset library browser. Source: `AssetLibraryTool.jsx`. The library lives
in `react-app/public/assetLibrary/`; import dumps (e.g. `react-app/2dAssetLibrary/`)
are gitignored and are **not** the library.

> [!note] Drift fixed by this vault
> Asset Library is the 20th registered tool but was **missing from CLAUDE.md's
> list** (which documented 19 / 13 art tools). The [[Tools]] Base is now derived
> from `registry.js`, the single source of truth.

Population workflow: [[Asset Library Import]] (agent runbook).
